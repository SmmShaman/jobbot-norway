name: Scheduled Job Scan

on:
  schedule:
    # Weekdays only (5 scans/week per user request); hourly trigger still
    # respects each user's individual scan_time_utc, so one real scan per day
    - cron: '0 * * * 1-5'
  workflow_dispatch: # Allow manual trigger
    inputs:
      force_run:
        description: 'Force run scan regardless of scheduled time'
        required: false
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'

jobs:
  trigger-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Scheduled Scanner Function
        run: |
          echo "Triggering scheduled-scanner at $(date -u)"
          echo "Event: ${{ github.event_name }}"

          # For manual runs, use the input value (default true)
          # For scheduled cron runs, don't force - let each user's scan_time_utc be respected
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            FORCE_RUN="${{ inputs.force_run }}"
          else
            FORCE_RUN="false"
          fi

          echo "Force run: $FORCE_RUN"

          response=$(curl -s -w "\n%{http_code}" -X POST \
            "https://db-jobbot.vitalii.no/functions/v1/scheduled-scanner" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -d "{\"source\": \"github-actions\", \"forceRun\": $FORCE_RUN}")

          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | sed '$d')

          echo "Response code: $http_code"
          echo "Response body: $body"

          if [ "$http_code" -ge 400 ]; then
            echo "Error: Function returned $http_code"
            exit 1
          fi

          echo "Scan triggered successfully!"
