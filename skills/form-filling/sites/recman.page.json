{
  "domain": "apply.recman.page",
  "notes": "Recman is a third-party application-form host used by multiple Norwegian employers (jobs are discovered via NAV/FINN, but the actual apply flow happens on apply.recman.page, not the job board). Test case: job 'Vi søker salgskonsulent B2B og B2C' (KARRIERESENTERET AS), apply URL https://apply.recman.page/job_post.php?id=482699&path=nav&sub_id=801. 4-step wizard, tabs labelled: Last opp CV / Profil / Spørsmål / Gjennomgå og søk.",
  "entryFlow": [
    { "step": "dismiss cookie banner", "selector": "text=Aksepter alle", "note": "getByText(exact:false) — may not appear on every load" },
    { "step": "click apply button", "selector": "text=Søk nå", "note": "getByText(exact:false).first() — opens the application dialog/wizard" }
  ],
  "steps": [
    {
      "name": "Last opp CV",
      "fields": [
        {
          "label": "Last opp CV",
          "selector": "input[type=\"file\"]",
          "type": "file",
          "gotcha": "plain native input, setInputFiles works directly; use a persistent local path (e.g. /workspace/agent/assets/cv.pdf), not /tmp"
        }
      ],
      "advanceButton": { "role": "button", "name": "Neste", "gotcha": "use getByRole, not getByText — getByText('Neste') false-matched an unrelated paragraph containing the word" }
    },
    {
      "name": "Profil",
      "fields": [
        { "label": "Fornavn*", "selector": "input[name=\"firstName\"]", "type": "text" },
        { "label": "Etternavn*", "selector": "input[name=\"lastName\"]", "type": "text" },
        { "label": "E-post*", "selector": "input[name=\"email\"]", "type": "text" },
        {
          "label": "phone (placeholder '+1 234 567 89 00')",
          "selector": "input[name=\"mobilePhone.number\"]",
          "type": "maskedPhone",
          "example": "pressSequentially('4792564334') -> renders as '+47 92564334'",
          "gotcha": "the .ApplyV2InputPhone__fields__select country dropdown (list items show English country names, e.g. 'Norway' not 'Norge') has ZERO effect on the actual value. The mask infers the calling code from the first digits typed. Ignore the dropdown entirely; type calling code + national number as one digit string directly into the number field."
        }
      ],
      "advanceButton": { "role": "button", "name": "Neste" }
    },
    {
      "name": "Spørsmål",
      "fields": [
        {
          "label": "per-job custom employer questions",
          "selector": "input[name^=\"text_\"]",
          "type": "text",
          "gotcha": "field names are dynamically generated per job posting (e.g. input[name=\"text_7905\"]); NOT stable across postings. The actual question text is only available via the associated <label> (e.g. 'Ønsker du å jobbe fulltid med salg og markedsføring?'). Answer is judgment-based per question, similar to cover-letter drafting — not a fixed value."
        }
      ],
      "advanceButton": { "role": "button", "name": "Neste" }
    },
    {
      "name": "Gjennomgå og søk",
      "fields": [
        {
          "label": "Søknadsbrev",
          "selector": ".ApplyV2Textarea__textarea[contenteditable]",
          "type": "contenteditable",
          "charLimit": 1500,
          "gotcha": "NOT a real <textarea> despite the visual appearance and an associated <label for=\"...\">. Actual DOM: <div class=\"ApplyV2Textarea__textarea\" contenteditable=\"plaintext-only\">. locator('textarea') finds nothing. Use assets/text-fields.mjs fillContentEditable (click then keyboard.type). Live counter shown in the label as e.g. '0/1500' — confirmed real limit is 1500 chars (not the 1400 originally assumed)."
        }
      ],
      "submitButton": { "role": "button", "name": "Søk" }
    }
  ],
  "verifiedRun": {
    "date": "2026-07-18",
    "applicationId": "f7c3acd8-5081-4f53-8d14-dd17e989d979",
    "jobId": "1396f6a9-3c1a-4ba6-843f-12908b33c98b",
    "result": "Phase 1 (fill-only, no submit) completed successfully: CV uploaded, profile fields filled, phone rendered correctly as +47 92564334, custom question answered, cover letter filled at 1221/1500 chars. Submit button 'Søk' was NOT clicked."
  }
}
